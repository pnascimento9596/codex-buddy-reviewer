# v0.5.0 stable-promotion readiness

Recorded on 2026-08-04 against protected `main` after final-closure Parts A–E.
This file is evidence, not authorization to promote.

## Exact identities

| Item | Value |
|---|---|
| Protected main (at ledger write) | `d10e08658b616e4369a009d8a6c3bd6dc76f9e43` |
| Preceding evidence merge (Part C docs/mode) | `ad57478bd97b18225d87617f5ffea0bca4c5ee1e` |
| Host evidence merge (Part A) | `c75ce957fa780fe8bb666d940ebce8210a102465` |
| Immutable rc.3 source | `0c6f09ae14d834fccb2a2289f6731511dbbdb034` |
| rc.3 tag object | `eb6a4541cbe75c73648da806ffacd22aef2b2f0d` |
| rc.3 distribution commit | `8782b70c5c664f5a73f9c225a802c71f1ff49ccf` |
| rc.3 tarball SHA-256 | `6de1ab3c4a47a96abbc5faf868d862d614a2e2a196fd4ec730f4b7c7b9d0b759` |
| rc.3 release run | `30861281580` |

## Part outcomes

### A — Native Codex host (rc.3 artifact)

- Host: Codex CLI `0.146.0` in terminal; Kitty `0.48.2` for pets. No GUI app required.
- Plugin: `0.5.0-rc.3+codex.20260803011343` from verified published tarball.
- Exact-final fallback proved twice (review identities `ce50eb…` and `e4a50a…`); both `no_findings`.
- **exact-final fallback safety observed**
- Speculative worker entered `capturing`, stayed 278.8s over multi-minute stable windows, then `failed` with zero launches — tracked by issue **#36** (not fixed in this lane).
- **speculative adoption blocked by #36 until observed on a real host post-fix**
- Five-pet gate: Bella/Lupo rendered; released buddy-byte/mochi/orbit not selectable on Codex 0.146.0; ambient error `Failed to load pet: load ambient pet` — issue **#38**.
- **five-pet host gate blocked by #38**
- First-enable baseline abstention fail-closed — issue **#37** (UX).
- Evidence: `docs/host-evidence/` (PR #40).

### B — Account / release governance

- Security Log (Safari, signed in as `pnascimento9596`):
  - rc.2 window `2026-08-01T09:20–10:20Z`: exactly three events at `09:38:46Z`, all GitHub System Copilot PR Reviewer OAuth rotations. No unexpected identities in-window.
  - rc.3 dispatch window `2026-08-03T23:00Z`–`2026-08-04T01:30Z` (covers release run `30861281580`): exactly two events at `2026-08-04T00:24:28Z`, both GitHub System Copilot PR Reviewer `oauth_access` regenerate/create. No unexpected identities in-window.
  - Evidence only under excluded `.local-evidence/security-log/` (never committed).
- `public-release` environment already requires sole reviewer `pnascimento9596` (`198005926`) with protected-branch policy. No mutation required. `can_admins_bypass: true` remains an owner decision (GOVERNANCE-TODO).
- Fine-grained PAT creation left as **owner residue** (agent must not handle generated secrets). Target: repo-scoped Contents + Pull requests write; no workflow dispatch; no deployment review.
- OAuth-app inventory, 2FA/email visibility remain owner-browser items.

### C — Exact-head two-family deep security scan

- Sealed empty-base partitions of exact head tree (prompt hard-cap 2 MiB).
- Reviewers: Claude Opus 4.8 high + Grok 4.5 high; `--confidence 0` for disposition retention.
- Provider calls used: 4 (of 6 budget).
- Accepted and fixed (PR #41 → `ad57478…`):
  - Human `mode status` omitted secondary reviewer and continuous-review state.
  - `docs/EVALUATION.md` abstain/deletion categories misdocumented corpus contracts.
- Rejected after live reproduction: `release.yml` `lookup_remote_tag_object` 404 path already proves absence (gh leaves error JSON on stdout).

### D — Deterministic timing (#14 / #25)

- Fixed in PR #42 → `d10e086…`.
- **#25**: chmod temp private-JSON inode before rename.
- **#14**: setup plan scan deadline 250ms → 5s.
- Local: 25× focused atomic/setup cleanup; 10× speculative checkpoint adoption. Issues #14 and #25 closed with evidence.

### E — OpenCode adapter

- Live: OpenCode `1.18.10`; `xai/grok-4.5` succeeds; `openai/gpt-5.6` returns upstream `UnknownError` server error (ChatGPT OAuth path), not isolation drift.
- Adapter fix (PR #42): accept exit codes `[0,1]` so structured JSONL error events are parsed instead of collapsing to generic `transport_exit`; safer error detail; doctor offline `opencode_cli_surface` PATH probe when OpenCode is configured.

## Still open before stable

| Gate | Tracker / note |
|---|---|
| Speculative receipt adoption on real host after worker fix | **#36** |
| Five-pet Codex selector / ambient load | **#38** |
| First-enable baseline UX | **#37** |
| Windows current-user-only DACL + live Windows provider egress | Deferred product gate |
| Owner: OAuth apps, PAT inventory, 2FA/email visibility, admin-bypass decision | GOVERNANCE-TODO |
| Fresh protected-main CI at promotion head | Re-verify at dispatch time |

## Promotion disposition

```text
exact-final fallback safety observed
speculative adoption blocked by #36 until observed on a real host post-fix
five-pet host gate blocked by #38
promotion dispatch awaits explicit owner instruction
```

Do **not** dispatch stable `v0.5.0` from this ledger alone.
