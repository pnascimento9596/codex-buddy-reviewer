# v0.5.0 stable-promotion readiness

Recorded on 2026-08-05 after published `v0.5.0-rc.4`, artifact-bound host-e2e,
and the post-rc.4 mechanics/security-scan lane (PRs #54, #55, #57).
This file is evidence, not authorization to promote.

## Exact identities

| Item | Value |
|---|---|
| Protected main (at ledger write) | `5d5c34f71a565d088d68318b36421e3347e33030` |
| Immutable rc.4 source | `b56224fb8e37ca02e071cda41702af4ef20f1ebb` |
| rc.4 tag object | `b5d8fbc47515b6ae0ab96491b4125cc6ea8f9149` |
| rc.4 distribution commit | `ac9f870f455645613c9f3dc8de4196fdc8a25d6d` |
| rc.4 tarball SHA-256 | `2ba45154d1e5c5218900b295412744ea9cfa1fe3a452085f9f77693651453df7` |
| rc.4 release run | `31013433126` (attempt 2 publish; attestations bind attempt 1) |
| #37 first-enable UX | `065c4704e7c5e37ff979b2a2daea67acf0163a16` (PR #50) |
| #36 keepalive | `866e359c53614ba4fa1c9a4bd0d4e1c32fb4ecdb` (PR #46) |
| #38 pet atlases | `23784a3a495ab09e60ac9283080b26410e5d744a` (PR #45) |
| Immutable rc.3 source | `0c6f09ae14d834fccb2a2289f6731511dbbdb034` |
| Release-tag reconcile | PR #54 → `bd9844622b462aa25b99bf1ce670aad9bb53250a` |
| Suite budget / host-e2e `.git` | PR #55 → `0df21e85df883f1a36f1140c9cd411a2503729c2` |
| Security-scan fixes | PR #57 → `5d5c34f71a565d088d68318b36421e3347e33030` |

## Stable gate matrix

| Gate | State | Evidence pointer |
|---|---|---|
| Protected CI at rc.4 source | **pass** | main validate `31011169822` attempt 2 on `b56224fb…` |
| rc.4 publication + attestations | **pass** | run `31013433126`; Layer E freeze PR #52 |
| Speculative adoption (published artifact) | **pass** | `docs/host-evidence/rc4-speculative-adoption-2026-08-05.txt` |
| Five-pet host-e2e v2 bundle vs published archive | **pass** | private validated bundle (5/5 pets); terminal pet selectors `docs/host-evidence/rc4-pet-selector-*.txt` |
| First-enable baseline UX (#37) | **pass** | PR #50; `rc4-enable-and-doctor.txt` |
| Supersession / stale-key rejection | **pass** | prior lane mismatched ready≠completed refused; recorded in VALIDATION Layer A |
| Prior host-verify provider overrun honesty | **recorded** | budget 4 used 5+; cause second #36 root cause mid-verify |
| Exact-head deep security scan | **required; open until promotion-head re-scan** | 6-call sealed scan on pre-fix head `bd98446` (tree `8cefadfb…`); reproduced defects fixed in PR #57 (`5d5c34f`). Final stable head must be re-scanned or the fix delta independently revalidated before promotion. |
| Release post-push tag reconcile | **pass** | PR #54; root cause run `31013433126` a1 REST race |
| publish=false dry-run after fix | **pass** | run `31042484160` on `bd98446`: validate+build success; attest/publish skipped; tags unchanged |
| Deterministic timing #14/#25/#29 | **closed; residual load family tracked** | PR #42 closed originals; PR #55 suite budget + process bounds; issue #56; 5× local full suite pass |
| Host-e2e marketplace `.git` contract | **pass** | PR #55; skip root `.git`; runbook updated |
| `HOST_EVIDENCE_V2_BUNDLE_GZIP_B64` | **set** | public-release env secret present (updated 2026-08-05T15:41:53Z) |
| rc.4 Security Log window | **pass (local-only)** | pad 13:45–15:00Z; 3 oauth_access events, owner only; GOVERNANCE-TODO updated |
| Windows current-user-only DACL + live Windows egress | **deferred** | product gate |
| Owner OAuth/PAT/2FA/email/admin-bypass | **open** | GOVERNANCE-TODO (Security Log multi-dispatch windows closed through rc.4) |
| Owner stable dispatch instruction | **required** | this ledger is not authorization |

## Step outcomes (post-rc.4 mechanics lane)

### Step 1 — release-workflow failure
- **Root cause:** post-push REST eventual consistency on `gh api …/git/ref/tags/…` after successful annotated-tag push (run `31013433126` attempt 1, ~322 ms after `[new tag]`/`Done`). Remote tag already equalled receipt `tag_object` `b5d8fbc…`; no Release until attempt 2.
- **Not:** annotated-vs-peeled comparison, wrong ref, or receipt field mismatch.
- **Fix (PR #54):** `scripts/ensure-release-tag.mjs` + `scripts/lib/release-tag-publish.mjs` — verify before push, idempotent already-present match, bounded post-push backoff, fail-closed genuine mismatch; adversarial `runCaptured` tests.

### Step 2 — dry-run exercise
- `publish=false` runs authorize + full validation + build; skips attest and publish (`if: inputs.publish`).
- Dispatch `31042484160` from protected main `bd98446`, `version=0.5.0-rc.4`, `publish=false`: all validation lanes + build **success**; attest **skipped**; publish **skipped**; tags still rc.1–rc.4 only.

### Step 3 — macOS cancellation
- **Mechanism:** `tests/automatic.test.mjs` file-level `testTimeoutFailure` at 900000 ms after ~897.6 s of completed work (PR #53 run `31018232912`; main `31020964234` a1). Aggregate duration, not a single hung test. Signature `fail 0 / cancelled 1`.
- Also process-family fails under load: deadline-cleanup pid race; hook 15 s deadline. Natural-exit force-kill held on that run.
- **Fix (PR #55):** test timeout 900→1200 s; deadline cleanup wait-for-pid; hook invoke 45 s.
- **Proof:** 5 consecutive full local `npm test` ALL PASS (2026-08-05T18:46–19:17Z).
- Issue **#56** tracks residual process/concurrency family.

### Step 4 — host-evidence contract
- Marketplace install is a **git clone** (`.git` present). Host-e2e binds **plugin payload** bytes to the published artifact and skips only root `.git`. Clean tarball extract is valid but not more representative once `.git` is excluded.
- Fixes `tree_file_oversized` from clone packs; `docs/HOST-E2E-RUNBOOK.md` records the contract.

### Step 5 — stable path mechanics
- **5a:** `HOST_EVIDENCE_V2_BUNDLE_GZIP_B64` is set on `public-release`.
- **5b:** rc.3→rc.4 tarball 13.66 MB→11.18 MB fully explained by atlas 8×11→8×9 (≈−2.49 MB uncompressed spritesheets). Both trees are **131 files** (130 `manifest.files` + `release-manifest.json`); path sets identical — no 130→131 path delta.
- **5c:** Security Log pad around rc.4: 3 `oauth_access` events at `14:18:55Z`, actor `pnascimento9596`, Copilot Chat, IP `104.28.57.116`. Local-only evidence under `.local-evidence/security-log/`.

### Step 6 — security-scan gate (restored REQUIRED)
- **Status for promotion:** **open**. The authorized 6-call sealed scan covered exact head `bd98446` / tree `8cefadfb…`. PR #57 then changed production paths (`src/filter-free-git.mjs`, `native/windows/job-supervisor.c`, `hooks/hooks.json`) on protected main `5d5c34f`. Those changes are the reproduced fixes (plus regression tests), not an unscanned feature dump — but they still mean the tree that would ship was not the tree sealed for the scan. Before stable promotion, re-run the exact-head scan on the final promotion head (or a sealed packet that includes the #57 delta with the same two-family budget rules).
- **Packets (scan head `bd98446`):** sealed empty-base partitions pA/pB/pC; all dry-run `truncated:false`; confidence 0.
- **Budget:** 6 calls used — Claude Opus 5 ×3 + Grok 4.5 ×3.

| # | Reviewer | Packet | Outcome | Disposition |
|---:|---|---|---|---|
| 1 | claude-opus-5 | pA runtime/scripts | 2 findings | **reproduced + fixed** (PR #57): Git `--source` named fail-closed; Windows ERROR exit 125 |
| 2 | claude-opus-5 | pB tests | 1 finding (test hygiene) | **unreproduced as product defect** (55 KB rev-list; passes) |
| 3 | claude-opus-5 | pC docs | docs/eval nits | EVALUATION claim false positive; GOVERNANCE rc.4 window recorded; skill `/buddy-review` wording intentional |
| 4 | grok-4.5 | pA | 2 findings | **reproduced + fixed** (PR #57): Stop/UserPromptSubmit hook timeouts raised to cover 1800 s / 180 s ceilings |
| 5 | grok-4.5 | pB | abstain | incomplete evidence for 11 paths — not a defect claim |
| 6 | grok-4.5 | pC | docs/skill nits | unreproduced as material product defects; low/medium doc clarity only |

- **PR #57** also answered Codex connector P1 on the fix PR: do not silently degrade historical `--source`; fail closed with `git_version_unsupported` when unknown-option.

### Attestation-binding note (rc.4)
Attestation certificates bind to run `31013433126` **attempt 1** (`runInvocationURI` …/attempts/1) because the attest job completed there. Publication of the GitHub Release completed on **attempt 2** after the tag already matched the receipt (partial-publish recovery). Attempt-1 attestations remain valid for the published artifact bytes; they are not rewritten on attempt 2. See `docs/VALIDATION.md` Layer E.

## Provider ledger (published rc.4 host-e2e)

| # | Call | Outcome |
|---:|---|---|
| 1 | doctor health | 1/1 pass ollama/glm-5.2:cloud |
| 2 | live multi-file review `425a466f…` | findings; adopted; 12888 ms |
| 3 | speculative sibling `06878449…` | findings; not Stop-presented; 14039 ms |

Budget was 1 doctor + 3 live; used 1+2 within budget.

## Residue before stable

1. Windows DACL + live Windows provider egress (deferred, reasoned)
2. Owner browser governance (OAuth apps, PAT inventory, 2FA/email, admin-bypass) — Security Log multi-dispatch windows through rc.4 closed
3. Explicit owner instruction to dispatch stable `v0.5.0`
4. Optional: residual process-family tracking (#56); doc nits from scan calls 3/6 (non-blocking)
5. Exact-head security re-scan (or sealed fix-delta revalidation) on the final promotion head before stable dispatch

## Promotion disposition

```text
published rc.4 verified (anonymous clone, assets, sidecars, three attestations)
speculative adoption observed on published artifact
five-pet host-e2e v2 bundle validated against published artifact
#37 first-enable baseline UX closed
release-tag post-push race fixed and dry-run exercised (publish=false)
exact-head security scan restored REQUIRED; defects found on bd98446 fixed in PR #57
promotion still requires scan/revalidation of the final promotion head (currently 5d5c34f+)
Windows DACL deferred; owner OAuth/PAT/2FA/admin-bypass open
promotion dispatch awaits explicit owner instruction
```

Do **not** dispatch stable `v0.5.0` from this ledger alone.
Do **not** treat this ledger as authorization for rc.5 either — PR #57 is source-fix material already on protected main; any further RC is a separate owner decision.
