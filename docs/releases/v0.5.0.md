# v0.5.0 stable release note

## Decision

Publish the first stable `v0.5.0` from the exact protected `main` promotion head
after version prep, host-evidence bind, and security-scan seal. This note is
not dispatch authorization by itself; dispatch follows the two-phase protocol
in `docs/releases/dispatch-attribution.md` under the owner's standing
2026-08-07 instruction.

## What lands relative to published rc.6

| Item | Content |
|---|---|
| PR #73 | Claude `auth_unavailable` classification; consent-lock test window; packaged win32-x64 Job Object helper |
| Host-e2e | rc.6 machine-captured evidence (#72); adoption proven Turn 3 |
| Operator purge | Multi-root purge (#66); purge-once-more after upgrade from rc.5 |

## Operator-facing purge note

On marketplace installs, automatic-review runtime state can live under
`~/.codex/plugins/data/<plugin-identity>/` while older CLI paths used
`~/.codex/codex-buddy-reviewer/`. **Operators who purged under rc.5 should purge
once more after upgrading to stable** (automatic mode disabled).

## Documented limitations

- Windows live provider egress: **SHIPS BLOCKED** (enforced; DACL post-stable).
- Stop >600 s host enforcement: **documented limitation** — hooks 210/1890
  configured; e2e beyond ~17.5 s not observed; rc.6 probe fail-closed on
  oversized evidence.
- Speculative adoption: **PROVEN** at rc.6 Turn 3.
- #62 consent-lock: characterized test-timing; product lock path closed by
  route enumeration.
- Five-pet host basis: machine-captured artifact-bound bundles (human-unreviewed).

## Protected publication gate

- [ ] Candidate PR passes local chain, secret scan, publication boundary, protected CI.
- [ ] Squash-merged; protected-main CI green at exact merge head (promotion head H*).
- [ ] Host-evidence v2 bundle bound to locally built 0.5.0 artifact; secret updated.
- [ ] Security-scan seal covers H* scan surface (two-family budget).
- [ ] Dispatch attribution Phase A external; `release.yml` `version=0.5.0` `publish=true` once.
- [ ] Post-publication ritual: anonymous clone, assets, attestations, Phase B.

The protected workflow, not this checklist, performs publication. No manual
source tag is created, moved, or deleted.
