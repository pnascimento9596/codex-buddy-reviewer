# v0.5.1 readiness ledger

Security-fix promotion ledger for the receipt-replay validation bypass (H07).
Recorded and updated through the lane-i session (2026-08-08).
This file is evidence, not authorization to dispatch.

## Exact identities

| Item | Value |
|---|---|
| Protected main at lane start | `07879a107e5a611f322b2a2c9e2500c8118bab78` (#83, gate lifted) |
| Published v0.5.0 artifact | tarball sha256 `0b474c5141ae55e6bb63d8555ae4f6498cc8ee5e2be0c6229f3d39f0cb0327e9`; source `d23890eb8e7f6cc92ce7b02a31240871b6e3e6b0` |
| H07 reproduction on v0.5.0 | lane-i `phase-0d-repro-v050.mjs` — 1 pass / 0 fail (forged pair replayed) |
| H07 classification | platform-independent; H01–H06 win32-only (derived from code, Phase 0c) |

## Fix delta (0.5.1)

| Item | Status |
|---|---|
| `validateAutomaticReceiptReplay` (automatic-receipt.mjs) | implemented |
| Replay branch wiring (lifecycle.mjs) | implemented — forged pair → `invalid_pre_review_receipt` |
| `WINDOWS_PROVIDER_EGRESS_GATE_LIFTED = false` | implemented — 0.5.1 never ships a lifted gate |
| Forged pair never replayed (test) | pass |
| Valid legacy receipt still replays (test) | pass |
| Production gate re-engaged (test) | pass |
| Full suite at candidate head | 846 pass / 0 fail / 30 skipped |
| check:syntax / types / assets / atlas / eval:validate / release:boundary | all pass |

## Remaining gates before dispatch

- [ ] Fix-delta security-scan packet (≤4 calls, two families) covering the delta.
- [ ] Host-evidence v2 bundle bound to locally built 0.5.1 artifact; `HOST_EVIDENCE_V2_BUNDLE_GZIP_B64` updated (write-only).
- [ ] Protected CI green on the exact promotion head after merge.
- [ ] Dispatch attribution Phase A external; `release.yml` `version=0.5.1` `publish=true` once; env approvals integer.
- [ ] Post-publication ritual (anonymous clone, assets, sidecars, attestations, Phase B).

## Posture notes

- v0.5.0 remains the latest published release until 0.5.1 publishes.
- The gate re-engagement is a release posture (ADR 0002 addendum), re-lifted for v0.6.0-rc.1 after H01–H06 remediation + Phase 4 re-seal.
- No dispatch performed by this ledger. Dispatch is governed by the standing 2026-08-07 owner instruction and the two-phase attribution protocol.
