# v0.6.0 release note

## Decision

`v0.6.0` was published from exact protected source `41b0fadbbbc85fcf3dca02192d002a4139dee504` after the Windows private-state remediation, replay fix, exact-tree review seal, bounded Windows viability experiment, provider opt-in implementation, documentation reconciliation, and protected cross-platform validation. The protected release workflow remains the only path that may create immutable release tags and Releases.

## What lands

- Windows x64 private-state DACL enforcement through the packaged protocol-2 helper.
- Explicit Windows provider-egress opt-in through `CODEX_BUDDY_WINDOWS_EGRESS_ENABLE=1`; the unset default remains fail-closed.
- Unconditional `CODEX_BUDDY_WINDOWS_EGRESS_BLOCK=1` kill switch.
- Assured runtime-root coverage, authorization ordering, kill-switch rechecks, typed provider-temporary integrity failures, and replay validation retained from the reviewed v0.6 candidate.
- Current public review guidance for the Lane J operator surface uses Claude, Ollama, and explicitly configured OpenCode connections. Grok is not part of this release lane or its review evidence.

## Exact evidence at publication

- Protected `main` at publication: `41b0fadbbbc85fcf3dca02192d002a4139dee504`.
- PR #95 opt-in merge: `40ecc13eab234d054c4a6c1c11f0e7752d58c3ba`; protected-main run `31518823351`, success.
- PR #96 documentation/provider-guidance merge: `257c62cb59578ba3a7bbd13e625f7ae7bc806bcb`; PR validation run `31521826253`, success.
- Final Windows-remediation tree previously sealed: `1a995b50eeb2df2637fb2b90b1cbe4211c734af6`.
- Final two-family seal manifest: SHA-256 `197ef5f587ac770cbbca98e51ba57793930e29e58ea9424428487a2862a696d0`.
- Packaged Windows x64 helper: SHA-256 `22884692f20edb592d57cb3ea00d03dfa857bfe148a9625c986916fa83110fe1`, 162816 bytes.

## Windows disposition

The two authorized Lane J branch-only Windows attempts (`31510839580`, `31511573245`) failed before Ollama setup, doctor, review, or provider inference. Provider calls, doctor calls, live turns, and inferences were all zero. The result is **NOT OBSERVED**, not provider failure. No third Windows avenue was used. Native Windows Codex host e2e and completed live Windows local-model review remain UNRUN; protected Windows Node 22/24 integration CI is the packaged-helper/DACL evidence.

## Publication record

- [x] Stable version candidate was committed with package and lockfile identity `0.6.0`.
- [x] Exact-head local suite, portable validators, secret scan, publication boundary, artifact verification, and distribution verification passed.
- [x] Bounded fix-delta review completed across the permitted Ollama Cloud and Codex/Claude review families; no Grok route was used.
- [x] Candidate PRs passed exact-head protected CI, review threads were reconciled, and merges were normal.
- [x] Protected-main validation passed at the exact promotion SHA.
- [x] Exact-artifact host evidence was validated and stored in the protected environment without exposing the secret value; visual entries retain the owner's explicit waiver rather than fabricated screenshot claims.
- [x] Phase A attribution bound `version=0.6.0`, `publish=true`, owner account `pnascimento9596`, and source `41b0fad…` externally.
- [x] `.github/workflows/release.yml` was dispatched exactly once from protected `main`: run `31596604260`.
- [x] Each separately presented `public-release` deployment was approved with integer environment IDs and read back: `5869549258`, `5869558664`, `5869562672`.
- [x] Release run identity, tag object, parentless distribution commit, assets, sidecars, and attestations were independently verified.
- [x] Anonymous post-publication installation and byte audit passed.

The workflow, not this document, created the tag and Release. No manual source tag was created, moved, or deleted.

## Remaining limitations

- Windows live provider viability is **NOT OBSERVED** after the two authorized setup failures; the default remains fail-closed and explicit opt-in is required.
- Native Windows Codex host e2e remains **UNRUN**. Protected Windows CI is the packaged-helper/DACL evidence.
- The host bundle is machine-captured and human-unreviewed. Transient visual worker/review states were not retained; no screenshot pass is claimed.
- Owner-only account controls (OAuth/PAT inventory, email/2FA review, and administrator-bypass policy) remain outside repository/API proof.
