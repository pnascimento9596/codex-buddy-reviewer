# v0.6.0 release note

## Decision

Prepare stable `v0.6.0` from the exact protected `main` promotion head after the Windows private-state remediation, replay fix, exact-tree review seal, bounded Windows viability experiment, provider opt-in implementation, documentation reconciliation, and protected cross-platform validation. This note is evidence, not dispatch authorization. The protected release workflow is the only path that may create the immutable tag and GitHub Release.

## What lands

- Windows x64 private-state DACL enforcement through the packaged protocol-2 helper.
- Explicit Windows provider-egress opt-in through `CODEX_BUDDY_WINDOWS_EGRESS_ENABLE=1`; the unset default remains fail-closed.
- Unconditional `CODEX_BUDDY_WINDOWS_EGRESS_BLOCK=1` kill switch.
- Assured runtime-root coverage, authorization ordering, kill-switch rechecks, typed provider-temporary integrity failures, and replay validation retained from the reviewed v0.6 candidate.
- Current public review guidance for the Lane J operator surface uses Claude, Ollama, and explicitly configured OpenCode connections. Grok is not part of this release lane or its review evidence.

## Exact evidence entering stable preparation

- Protected `main` entering version preparation: `257c62cb59578ba3a7bbd13e625f7ae7bc806bcb`.
- PR #95 opt-in merge: `40ecc13eab234d054c4a6c1c11f0e7752d58c3ba`; protected-main run `31518823351`, success.
- PR #96 documentation/provider-guidance merge: `257c62cb59578ba3a7bbd13e625f7ae7bc806bcb`; PR validation run `31521826253`, success.
- Final Windows-remediation tree previously sealed: `1a995b50eeb2df2637fb2b90b1cbe4211c734af6`.
- Final two-family seal manifest: SHA-256 `197ef5f587ac770cbbca98e51ba57793930e29e58ea9424428487a2862a696d0`.
- Packaged Windows x64 helper: SHA-256 `22884692f20edb592d57cb3ea00d03dfa857bfe148a9625c986916fa83110fe1`, 162816 bytes.

## Windows disposition

The two authorized Lane J branch-only Windows attempts (`31510839580`, `31511573245`) failed before Ollama setup, doctor, review, or provider inference. Provider calls, doctor calls, live turns, and inferences were all zero. The result is **NOT OBSERVED**, not provider failure. No third Windows avenue was used. Native Windows Codex host e2e and completed live Windows local-model review remain UNRUN; protected Windows Node 22/24 integration CI is the packaged-helper/DACL evidence.

## Publication gate

- [ ] Stable version candidate is committed on a clean branch with package and lockfile identity `0.6.0`.
- [ ] Exact-head local suite, portable validators, secret scan, publication boundary, artifact verification, and distribution verification pass.
- [ ] Bounded fix-delta review is complete across the permitted Ollama Cloud and Codex/Claude review families, with all verdicts bound to the same final source tree.
- [ ] Candidate PR passes exact-head protected CI, has no unresolved review threads, and merges normally.
- [ ] Protected-main validation passes at the exact promotion SHA and local `main` is synchronized.
- [ ] Exact macOS host-evidence bundle is generated from the final `0.6.0` artifact, validated, and stored in the protected environment secret without exposing its value.
- [ ] Phase A attribution records the exact source SHA, `version=0.6.0`, `publish=true`, owner account, authority, and UTC window externally.
- [ ] `.github/workflows/release.yml` is dispatched exactly once from unchanged protected `main`.
- [ ] Each separately presented `public-release` deployment is approved with integer environment IDs and read back.
- [ ] Release run identity, tag object, parentless distribution commit, assets, sidecars, and attestations are independently verified.
- [ ] Anonymous post-publication installation and byte audit pass.

The workflow, not this document, creates the tag or Release. No manual source tag is created, moved, or deleted.
