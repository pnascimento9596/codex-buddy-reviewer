interface:
  display_name: "Buddy Review"
  short_description: "Independent Claude or Ollama review of local changes"
  default_prompt: "Use $review with an explicitly selected Claude or Ollama connection to independently review my current changes."
policy:
  allow_implicit_invocation: false
